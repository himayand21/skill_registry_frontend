import {
    NAME,
    EMAIL,
    PASSWORD,
    MANAGER_ID
} from './userConstants';

const MANAGER_OPTIONS = 'managerOptions';

const SIGNUP_KEYS = [
    NAME,
    EMAIL,
    PASSWORD,
    MANAGER_ID
];

export {
    SIGNUP_KEYS,
    MANAGER_OPTIONS
}